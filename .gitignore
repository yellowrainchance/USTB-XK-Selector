# ---- 运行时产物 / 个人数据（严禁提交） ----
config.json            # 含真实 SESSION，仅本机使用
enum_cache.json        # 选课枚举本地缓存
logs/                  # 运行日志（每次启动一个文件）
_ui_assets/            # 运行时生成的图标缓存

# ---- PyInstaller 打包产物 ----
build/
dist/
*.spec

# ---- Python ----
__pycache__/
*.pyc
.venv/
venv/
